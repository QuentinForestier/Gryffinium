package graphical;

public enum GraphicalElementType
{
    // Entities
    CLASS,
    INNER_CLASS,
    ASSOCIATION_CLASS,
    ENUM,
    INTERFACE,
    INNER_INTERFACE,

    // Links
    BINARY_ASSOCIATION,
    MUTLI_ASSOCIATION,
    AGGREGATION,
    COMPOSITION,
    DEPENDENCY,
    GENEREALIZATION,
    REALIZATION,
    INNER,

    // Enum Values
    VALUE,

    // Attributes
    ATTRIBUTE,
    PARAMETER,

    // Operations
    CONSTRUCTOR,
    METHOD;


}
